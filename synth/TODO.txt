let complexity = Math.abs(config.R - config.r) + config.ratio * 10;

// normalise it roughly
let t = Math.min(complexity / 500, 1);

let min = 100;
let max = 2000;

let freq = min * Math.pow(max / min, t);
audio.setFreq(freq);

0.0 → silence

1.0 → full volume (no change)

let g = 0.02 + config.speed * 0.1;
g = Math.min(g, 0.2);
audio.setGain(g);

gain = Math.pow(sliderValue, 2) * 0.2;

let notes = [220, 247, 261, 293, 329, 349, 392, 440]; // A minor-ish
freq = notes[Math.floor(t * notes.length)];