export default class Renderer {
    constructor() {}

    animateSpin(result) {
        console.log(result);
        return Promise.resolve();
    }
    animatePayout(payout) {
        console.log(payout);
        return Promise.resolve();
    }
}