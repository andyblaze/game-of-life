import { lerpColor } from "./functions.js";

class VoronoiLifespan {
    update(site) {
        const a =  aurora;
        a.age++;
        if (a.age >= a.lifespan) {
            // if we're doing age related stuff
        }
        else {
            // do other stuff
        }
    }
}
export class Type1Lifespan extends AuroraLifespan {}
export class Type2Lifespan extends AuroraLifespan {}
export class Type3Lifespan extends AuroraLifespan {}

